package com.github.jaykkumar01.vaultspace.views.popups.old.core;

public enum ModalType {
    STATE,   // dismiss clears all specs of this SAME spec type
    EVENT    // dismiss affects only that spec instance
}

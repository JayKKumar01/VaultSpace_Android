<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/vs_ripple_primary">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp"/>

            <!-- Slightly brighter than vs_surface_soft -->
            <solid android:color="@color/vs_toggle_off"/>

            <!-- Thin accent border -->
            <stroke
                android:width="1dp"
                android:color="@color/vs_accent_primary"/>
        </shape>
    </item>

</ripple>
